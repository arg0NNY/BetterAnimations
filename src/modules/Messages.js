
const Messages = {
  SELECT_ANIMATION_TO_ENABLE: 'Select animation to enable',
  FORCE_DISABLED_BY_ANIMATION: animationName => `Force disabled by selected animation (${animationName})`
}

export default Messages
